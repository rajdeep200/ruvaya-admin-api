import{z}from"zod";
export const cartLineSchema=z.object({productId:z.string().min(1),productSlug:z.string().min(1),colorId:z.string().min(1),size:z.string().min(1),quantity:z.number().int().min(1).max(10)});
export const cartRequestSchema=z.object({lines:z.array(cartLineSchema).min(1).max(50),couponCode:z.string().trim().max(40).optional(),pincode:z.string().regex(/^[1-9]\d{5}$/).optional()});
export const addressSchema=z.object({fullName:z.string().min(2).max(120),phone:z.string().regex(/^[6-9]\d{9}$/),email:z.string().email(),addressLine:z.string().min(5).max(300),locality:z.string().max(100).optional(),landmark:z.string().max(100).optional(),city:z.string().min(2).max(80),state:z.string().min(2).max(80),pincode:z.string().regex(/^[1-9]\d{5}$/),deliveryInstructions:z.string().max(200).optional()});
export const checkoutSchema=z.object({idempotencyKey:z.string().uuid(),address:addressSchema,lines:z.array(cartLineSchema).min(1).max(50),couponCode:z.string().max(40).optional(),affiliateCode:z.string().max(80).optional(),utm:z.object({source:z.string().max(100).optional(),medium:z.string().max(100).optional(),campaign:z.string().max(100).optional(),content:z.string().max(100).optional(),term:z.string().max(100).optional()}).optional()});
