import{handleError,ok,ApiError}from"@/lib/http/api";import{prisma}from"@/lib/db/prisma";export async function GET(_:Request,{params}:{params:Promise<{slug:string}>}){try{const p=await prisma.contentPage.findFirst({where:{slug:(await params).slug,active:true},include:{versions:{where:{status:"PUBLISHED"},orderBy:{version:"desc"},take:1}}});if(!p||!p.versions[0])throw new ApiError("NOT_FOUND","Content page not found",404);return ok({slug:p.slug,title:p.title,content:p.versions[0].content,updatedAt:p.versions[0].createdAt.toISOString()})}catch(e){return handleError(e)}}
