import{z}from"zod";import{handleError,ok,parseJson}from"@/lib/http/api";import{requestPasswordReset}from"@/modules/auth/service";export async function POST(r:Request){try{const{email}=z.object({email:z.string().email()}).parse(await parseJson(r));const developmentToken=await requestPasswordReset(email);return ok({accepted:true,message:"If the account exists, a reset email will be sent.",...(developmentToken?{developmentToken}:{})})}catch(e){return handleError(e)}}
