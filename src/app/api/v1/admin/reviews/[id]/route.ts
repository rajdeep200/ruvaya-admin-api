import{z}from"zod";import{ReviewStatus}from"@prisma/client";import{handleError,ok,parseJson}from"@/lib/http/api";import{requireAdmin}from"@/modules/auth/service";import{prisma}from"@/lib/db/prisma";import{audit}from"@/modules/audit/service";export async function PATCH(r:Request,{params}:{params:Promise<{id:string}>}){try{const admin=await requireAdmin("reviews.write");const input=z.object({status:z.nativeEnum(ReviewStatus).optional(),featured:z.boolean().optional(),adminReply:z.string().max(2000).optional()}).parse(await parseJson(r));const id=(await params).id;const before=await prisma.review.findUniqueOrThrow({where:{id}});const after=await prisma.$transaction(async tx=>{const v=await tx.review.update({where:{id},data:{status:input.status,featured:input.featured}});if(input.adminReply)await tx.reviewReply.create({data:{reviewId:id,body:input.adminReply,adminUserId:admin.id}});return v});await audit({adminUserId:admin.id,action:"REVIEW_MODERATED",entityType:"Review",entityId:id,before,after});return ok(after)}catch(e){return handleError(e)}}
