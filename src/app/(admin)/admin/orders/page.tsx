import Link from"next/link";import{prisma}from"@/lib/db/prisma";import{formatPaise}from"@/lib/money";export default async function Orders(){const rows=await prisma.order.findMany({include:{customer:true},orderBy:{createdAt:"desc"},take:100});return<><h1>Orders</h1><p className="muted">Payment, sourcing and fulfilment queue</p><div className="card"><table><thead><tr><th>Order</th><th>Customer</th><th>Status</th><th>Total</th><th>Placed</th></tr></thead><tbody>{rows.map(o=><tr key={o.id}><td><Link href={`/admin/orders/${o.id}`}>{o.orderNumber}</Link></td><td>{o.customer.name}</td><td><span className="pill">{o.status}</span></td><td>{formatPaise(o.totalPaise)}</td><td>{o.createdAt.toLocaleDateString("en-IN")}</td></tr>)}</tbody></table>{!rows.length&&<p className="muted">No orders yet.</p>}</div></>}
