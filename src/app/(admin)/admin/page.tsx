import{prisma}from"@/lib/db/prisma";import{formatPaise}from"@/lib/money";export default async function Dashboard(){const[products,orders,pending,revenue]=await Promise.all([prisma.product.count({where:{deletedAt:null}}),prisma.order.count(),prisma.order.count({where:{status:{in:["PAID","CONFIRMED","SOURCING","READY_TO_SHIP"]}}}),prisma.order.aggregate({_sum:{totalPaise:true},where:{status:{in:["PAID","CONFIRMED","SOURCING","READY_TO_SHIP","SHIPPED","DELIVERED"]}}})]);return<><h1>Good day</h1><p className="muted">A focused view of catalogue and fulfilment health.</p><section className="grid" aria-label="Key metrics"><div className="card metric">Products<strong>{products}</strong></div><div className="card metric">Orders<strong>{orders}</strong></div><div className="card metric">Needs action<strong>{pending}</strong></div><div className="card metric">Recorded revenue<strong>{formatPaise(revenue._sum.totalPaise??0)}</strong></div></section></>}
