import Link from"next/link";import{notFound}from"next/navigation";import{prisma}from"@/lib/db/prisma";import{formatPaise}from"@/lib/money";export default async function Product({params}:{params:Promise<{id:string}>}){const p=await prisma.product.findUnique({where:{id:(await params).id},include:{variants:true,media:true,measurements:true,collections:{include:{collection:true}}}});if(!p)notFound();return<><div className="header"><div><h1>{p.name}</h1><p className="muted">{p.internalCode} · {p.status}</p></div><Link className="btn" href={`/admin/products/${p.id}/edit`}>Edit</Link></div><div className="grid"><div className="card metric">Price<strong>{formatPaise(p.salePricePaise??p.regularPricePaise)}</strong></div><div className="card metric">Variants<strong>{p.variants.length}</strong></div><div className="card metric">Available stock<strong>{p.variants.reduce((n,v)=>n+v.currentStock-v.reservedStock,0)}</strong></div><div className="card metric">Media<strong>{p.media.length}</strong></div></div><div className="card" style={{marginTop:16}}><h2>Description</h2><p>{p.fullDescription}</p><h2>Variants</h2><table><thead><tr><th>SKU</th><th>Colour</th><th>Size</th><th>Stock</th></tr></thead><tbody>{p.variants.map(v=><tr key={v.id}><td>{v.sku}</td><td>{v.colorLabel}</td><td>{v.size}</td><td>{v.currentStock-v.reservedStock}</td></tr>)}</tbody></table></div></>}
