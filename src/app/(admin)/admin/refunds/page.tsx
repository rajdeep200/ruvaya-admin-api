import{prisma}from"@/lib/db/prisma";import{formatPaise}from"@/lib/money";import{formatIst}from"@/lib/datetime";export default async function Refunds(){const rows=await prisma.refund.findMany({include:{order:true},orderBy:{createdAt:"desc"},take:100});return<><h1>Refunds</h1><div className="card"><table><thead><tr><th>Order</th><th>Status</th><th>Amount</th><th>Reason</th><th>Created</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.order.orderNumber}</td><td><span className="pill">{x.status}</span></td><td>{formatPaise(x.amountPaise)}</td><td>{x.reason}</td><td>{formatIst(x.createdAt)}</td></tr>)}</tbody></table>{!rows.length&&<p className="muted">No refunds yet.</p>}</div></>}
